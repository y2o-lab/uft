const CACHE="uft-static-v1";self.addEventListener("install",event=>event.waitUntil(caches.open(CACHE).then(cache=>cache.addAll(["/","/manifest.webmanifest","/icon.svg"])))),self.addEventListener("activate",event=>event.waitUntil(self.clients.claim())),self.addEventListener("fetch",event=>{if("GET"!==event.request.method||new URL(event.request.url).origin!==self.location.origin)return;event.respondWith(caches.match(event.request).then(cached=>cached||fetch(event.request).then(response=>(response.ok&&"document"!==event.request.destination&&void caches.open(CACHE).then(cache=>cache.put(event.request,response.clone())),response)).catch(()=>caches.match("/")))});
